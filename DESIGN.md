# 植物馆展览系统设计方案

## 1. 系统定位

本系统是一个基于动态网页技术的植物展览管理系统，重点完成以下三部分内容：

1. 植物资料收录
2. 植物资料搜索与查看
3. 基于照片的植物检索

系统面向普通浏览用户和资料维护人员，普通用户可以浏览、搜索植物资料并上传照片进行检索；维护人员可以录入、修改、删除植物资料。

---

## 2. 技术选型

### 前端

- Vue 3
- Element Plus
- Vite

用于实现动态页面、组件化界面和交互式图片上传。

### 后端

- Python 3.11+
- FastAPI

用于提供 RESTful API，包括植物资料、搜索、图片上传与图像匹配接口。

### 数据库

- SQLite

适合毕业设计阶段的本地开发和演示。后续如需多用户部署，可迁移到 MySQL 或 PostgreSQL。

### 图像检索方案

采用“图像特征 + 相似度匹配”的方式：

1. 将数据库中每张植物图片提取为特征向量；
2. 用户上传图片后，同样提取特征向量；
3. 计算上传图片与数据库图片之间的余弦相似度；
4. 按相似度从高到低返回对应的植物信息。

第一阶段可以使用 ResNet50 提取图像特征，后续如果需要更准确的语义匹配，可以替换为 CLIP 模型。

---

## 3. 系统架构

```text
用户浏览器
    |
    | HTTP / REST API
    v
Vue 3 前端
    |
    | Axios / Fetch
    v
FastAPI 后端
    |
    +-- 植物资料接口
    +-- 搜索接口
    +-- 图片上传接口
    +-- 图像特征匹配接口
    |
    v
SQLite 数据库 / 图片特征索引
```

系统主要分为三层：

1. 展示层：负责页面布局、交互和数据展示；
2. 业务层：负责植物资料管理、搜索、图片匹配；
3. 数据层：保存植物基础信息、图片信息、分类信息。

---

## 4. 功能模块设计

### 4.1 植物资料收录模块

功能：

- 新增植物资料；
- 编辑植物资料；
- 删除植物资料；
- 上传或关联植物图片；
- 管理植物分类。

字段包括：

| 字段 | 说明 |
| --- | --- |
| id | 植物编号 |
| name | 植物名称 |
| scientific_name | 学名 |
| category | 分类，例如观叶植物、多肉植物、攀援植物 |
| description | 植物简介 |
| image_path | 主图路径 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 4.2 植物搜索与查看模块

功能：

- 按名称搜索；
- 按学名搜索；
- 按分类筛选；
- 按关键词模糊搜索；
- 查看植物详情；
- 展示植物图片、简介、学名、分类等信息。

### 4.3 照片检索模块

用户操作流程：

1. 用户进入图片检索页面；
2. 上传植物照片；
3. 后端接收图片；
4. 后端提取上传图片的特征向量；
5. 与数据库中已有植物图片特征计算相似度；
6. 返回相似度最高的若干植物；
7. 前端展示匹配结果；
8. 用户点击结果可以进入植物详情页。

匹配流程：

```text
上传图片
   |
   v
图像预处理
   |
   v
ResNet50 / CLIP 提取特征
   |
   v
与数据库图片特征计算余弦相似度
   |
   v
按相似度排序
   |
   v
返回匹配植物列表
```

---

## 5. 页面设计

### 5.1 首页

展示系统标题、植物馆简介、热门植物推荐、功能入口。

### 5.2 植物列表页

展示所有植物卡片，包含图片、名称、学名、分类。

支持：

- 关键词搜索；
- 分类筛选；
- 点击进入详情页。

### 5.3 植物详情页

展示：

- 植物名称；
- 学名；
- 分类；
- 图片；
- 植物简介；
- 生长习性；
- 展示位置。

### 5.4 图片检索页

提供图片上传区域，用户上传后显示：

- 上传图片预览；
- 系统识别/匹配中的植物；
- 相似度；
- 植物详情入口。

### 5.5 资料管理页

供维护人员使用，功能包括：

- 新增植物；
- 编辑植物；
- 删除植物；
- 上传图片；
- 管理分类。

---

## 6. 数据库设计

### plants 表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER | 主键 |
| name | TEXT | 植物名称 |
| scientific_name | TEXT | 学名 |
| category | TEXT | 分类 |
| description | TEXT | 简介 |
| image_path | TEXT | 图片路径 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### plant_images 表

用于支持同一植物关联多张图片，提高图像检索准确率。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER | 主键 |
| plant_id | INTEGER | 植物 id |
| image_path | TEXT | 图片路径 |
| feature_path | TEXT | 特征文件路径 |
| created_at | DATETIME | 创建时间 |

### categories 表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER | 主键 |
| name | TEXT | 分类名称 |

---

## 7. API 设计

### 7.1 植物资料

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/plants` | 获取植物列表 |
| GET | `/api/plants/{id}` | 获取植物详情 |
| POST | `/api/plants` | 新增植物 |
| PUT | `/api/plants/{id}` | 修改植物 |
| DELETE | `/api/plants/{id}` | 删除植物 |

### 7.2 搜索

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/plants?q=keyword` | 关键词搜索 |
| GET | `/api/plants?category=观叶植物` | 分类筛选 |

### 7.3 图片检索

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/identify` | 上传图片并返回相似植物 |

请求体使用 `multipart/form-data`，字段名为 `file`。

返回结果示例：

```json
[
  {
    "plant": {
      "id": 1,
      "name": "绿萝",
      "scientific_name": "Epipremnum aureum",
      "category": "观叶植物"
    },
    "score": 0.932
  }
]
```

---

## 8. 目录结构

```text
plant-exhibition-web/
├─ backend/
│  ├─ main.py
│  ├─ database.py
│  ├─ models.py
│  ├─ schemas.py
│  ├─ image_features.py
│  └─ seed.py
├─ frontend/
│  ├─ index.html
│  ├─ js/
│  └─ css/
├─ requirements.txt
├─ README.md
└─ DESIGN.md
```

后续如果升级为完整 Vite 工程，前端可以调整为：

```text
frontend/
├─ src/
│  ├─ main.js
│  ├─ App.vue
│  ├─ router/
│  ├─ views/
│  └─ components/
└─ package.json
```

---

## 9. 开发顺序

1. 搭建前后端项目骨架；
2. 设计数据库表结构；
3. 实现植物资料增删改查接口；
4. 实现植物列表和详情页面；
5. 实现关键词搜索；
6. 实现图片上传接口；
7. 接入图像特征提取和相似度匹配；
8. 完成图片检索结果展示；
9. 增加管理端资料维护功能；
10. 测试、优化和整理文档。

---

## 10. 演示与验收方式

演示时重点展示：

1. 用户进入植物列表页；
2. 输入关键字搜索植物；
3. 点击植物卡片查看详情；
4. 上传一张植物照片；
5. 系统返回相似植物；
6. 点击匹配结果进入对应植物详情。

演示环境可以使用本地浏览器访问：

```text
http://127.0.0.1:8000
```
