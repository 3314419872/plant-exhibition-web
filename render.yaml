services:
  - type: web
    name: plant-exhibition-web
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash -c "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
